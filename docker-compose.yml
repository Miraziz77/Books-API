volumes:
  db_sqlite3:
  images:

services:
  backend:
    build: ./
    env_file: .env
    volumes:
      - .:/app
      - db_sqlite3:/data/
      - images:/app/api/parsing_json/images/
    ports:
      - "8000:8000"